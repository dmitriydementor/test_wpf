<Window x:Class="LearnIT.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         Title="LearnIT"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}" FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        MinHeight="550" MinWidth="700"
       Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BitmapImage x:Key="AboutImage" UriSource="about.jpg"></BitmapImage>
    </Window.Resources>
    <Grid>
        <Grid.Background>
            <LinearGradientBrush>
                <GradientStop Offset="0.00" Color="#FFF9EBD7"></GradientStop>
                <GradientStop Offset="1.00" Color="#FFF8DAFD"></GradientStop>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Image Height="400" Grid.Row="0" Source="../about.jpg" Margin="50"></Image>
        <TextBlock FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" Margin="30 10">Created by Dmitriy Koshevets, 1BS-14b VNTU</TextBlock>
    </Grid>
</Window>
